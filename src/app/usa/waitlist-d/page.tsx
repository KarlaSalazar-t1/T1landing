import T1WaitlistD from "@/components/T1WaitlistD";

export const metadata = {
  title: "T1 · Join the waitlist",
  description:
    "Built to power the next generation of commerce. Join the T1 waitlist — the all-in-one, AI-native platform to sell, get paid, and ship.",
};

export default function USAWaitlistDPage() {
  return <T1WaitlistD />;
}
